version: "1.0"
on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: debian
    steps:
      - uses: checkout
      - uses: static-hosting
        with:
          src: ./
          index: index.html
