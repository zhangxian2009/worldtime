{
  "name": "worldtime",
  "version": "1.3.0",
  "description": "Global time visualization with map / timeline / comparison / clock TABs",
  "private": true,
  "scripts": {
    "start":     "npx http-server . -p 8080 -c-1",
    "typecheck": "tsc -p jsconfig.json --noEmit",
    "test":      "vitest run",
    "test:watch":"vitest"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^4.1.5"
  }
}
